# README assets

The root `README.md` references two images here. Add them so they render inline:

- `ralph-wiggum.png` — a Ralph Wiggum still (top of the README).
- `ralph-loop.png` — Ralph "at the wheel" (the Ralph Loop section).

Any Ralph Wiggum PNG works — these are just for flavor. Keep them reasonably small (≤ ~300px wide).
